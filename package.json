{
  "name": "markdown-studio",
  "displayName": "Markdown Studio",
  "description": "Local-first Markdown authoring studio with integrated diagram rendering (Mermaid, PlantUML), SVG support, and secure PDF export.",
  "version": "0.3.0",
  "publisher": "local",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kino-6/vscode-markdown-studio.git"
  },
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Programming Languages",
    "Visualization",
    "Formatters"
  ],
  "keywords": [
    "markdown",
    "pdf",
    "mermaid",
    "plantuml",
    "preview"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "icon": "icon/icon.png",
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markdownStudio.openPreview",
        "title": "Markdown Studio: Preview"
      },
      {
        "command": "markdownStudio.exportPdf",
        "title": "Markdown Studio: Export PDF"
      },
      {
        "command": "markdownStudio.validateEnvironment",
        "title": "Markdown Studio: Validate Local Environment"
      },
      {
        "command": "markdownStudio.reloadPreview",
        "title": "Markdown Studio: Reload Preview (Clear Cache)"
      },
      {
        "command": "markdownStudio.setupDependencies",
        "title": "Markdown Studio: Setup Dependencies"
      },
      {
        "command": "markdownStudio.insertToc",
        "title": "Markdown Studio: Insert TOC"
      }
    ],
    "configuration": {
      "title": "Markdown Studio",
      "properties": {
        "markdownStudio.plantuml.mode": {
          "type": "string",
          "default": "bundled-jar",
          "enum": [
            "bundled-jar",
            "external-command",
            "docker"
          ],
          "description": "PlantUML rendering mode. MVP only fully supports bundled-jar."
        },
        "markdownStudio.java.path": {
          "type": "string",
          "default": "java",
          "description": "Path to Java executable used for PlantUML rendering."
        },
        "markdownStudio.export.pageFormat": {
          "type": "string",
          "default": "A4",
          "enum": [
            "A3",
            "A4",
            "A5",
            "Letter",
            "Legal",
            "Tabloid"
          ],
          "description": "Page format for PDF export."
        },
        "markdownStudio.security.blockExternalLinks": {
          "type": "boolean",
          "default": true,
          "description": "Block external links and remote resources in preview/export by default.",
          "deprecationMessage": "非推奨: markdownStudio.security.externalResources.mode を使用してください"
        },
        "markdownStudio.security.externalResources.mode": {
          "type": "string",
          "default": "whitelist",
          "enum": [
            "block-all",
            "whitelist",
            "allow-all"
          ],
          "enumDescriptions": [
            "Block all external resources",
            "Allow only whitelisted domains",
            "Allow all external resources"
          ],
          "description": "外部リソースの制御モード"
        },
        "markdownStudio.security.externalResources.allowedDomains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "github.com",
            "raw.githubusercontent.com",
            "user-images.githubusercontent.com"
          ],
          "description": "ホワイトリストモードで許可するドメインのリスト"
        },
        "markdownStudio.export.header.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable header in PDF export. When enabled, a header with the document title is added to each page."
        },
        "markdownStudio.export.header.template": {
          "type": ["string", "null"],
          "default": null,
          "description": "Custom HTML template for the PDF export header. Use Playwright CSS classes (pageNumber, totalPages, title, date, url) for dynamic content. When null, the built-in default template is used."
        },
        "markdownStudio.export.footer.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable footer in PDF export. When enabled, a footer with page numbers is added to each page."
        },
        "markdownStudio.export.footer.template": {
          "type": ["string", "null"],
          "default": null,
          "description": "Custom HTML template for the PDF export footer. Use Playwright CSS classes (pageNumber, totalPages, title, date, url) for dynamic content. When null, the built-in default template is used."
        },
        "markdownStudio.export.pageBreak.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable CSS page-break support in PDF export. When enabled, page-break-before and page-break-after CSS properties are honored."
        },
        "markdownStudio.preview.sourceJump.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Double-click in preview jumps to the corresponding source line."
        },
        "markdownStudio.style.preset": {
          "type": "string",
          "default": "markdown-pdf",
          "enum": [
            "markdown-pdf",
            "github",
            "minimal",
            "academic",
            "custom"
          ],
          "description": "Style preset for preview and PDF export."
        },
        "markdownStudio.style.fontFamily": {
          "type": "string",
          "default": "-apple-system, BlinkMacSystemFont, \"Segoe UI\", Helvetica, Arial, sans-serif",
          "description": "Font family for body text in preview and PDF export."
        },
        "markdownStudio.style.fontSize": {
          "type": "number",
          "default": 14,
          "description": "Font size in pixels for body text in preview and PDF export (clamped to 8–32)."
        },
        "markdownStudio.style.lineHeight": {
          "type": "number",
          "default": 1.6,
          "description": "Line height for body text in preview and PDF export (clamped to 1.0–3.0)."
        },
        "markdownStudio.export.margin": {
          "type": "string",
          "default": "20mm",
          "description": "Page margin for PDF export. Accepts CSS units (mm, cm, in, px)."
        },
        "markdownStudio.toc.levels": {
          "type": "string",
          "default": "1-3",
          "description": "TOCに含める見出しレベルの範囲（例: '1-3', '2-4'）"
        },
        "markdownStudio.toc.orderedList": {
          "type": "boolean",
          "default": false,
          "description": "TOCを番号付きリスト（ol）で生成する"
        },
        "markdownStudio.toc.pageBreak": {
          "type": "boolean",
          "default": true,
          "description": "PDFエクスポート時にTOCの前後に改ページを挿入する"
        },
        "markdownStudio.codeBlock.lineNumbers": {
          "type": "boolean",
          "default": false,
          "description": "コードブロックに行番号を表示する"
        }
      }
    }
  },
  "scripts": {
    "build": "node config/esbuild.js",
    "watch": "node config/esbuild.js --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run -c config/vitest.unit.config.ts",
    "test:integration": "vitest run -c config/vitest.integration.config.ts",
    "pretest:e2e": "tsc -p config/tsconfig.e2e.json && npm run build",
    "test:e2e": "vscode-test",
    "test:ci": "npm run lint && npm run test:unit && npm run test:integration",
    "package": "npm run build && rm -f dist/*.vsix && vsce package -o dist/",
    "reinstall": "bash dev_reinstall.sh"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.0",
    "playwright": "^1.53.0",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "@types/sanitize-html": "^2.16.0",
    "@types/vscode": "^1.92.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.24.0",
    "fast-check": "^4.6.0",
    "mermaid": "^11.12.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
